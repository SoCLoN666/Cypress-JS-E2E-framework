name: Chrome

on: push

jobs:
    chrome:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout
              uses: actions/checkout@v1
            - name: Chrome
              uses: cypress-io/github-action@v2
              timeout-minutes: 10
              with:
                  start: npm start
                  browser: chrome
